import { DyFormFieldDirective } from './dy-form-field.directive';

describe('DyFormFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new DyFormFieldDirective();
    expect(directive).toBeTruthy();
  });
});
